$('#dialog').slideToggle();
$('#contact_<%=@contact.id%>').replaceWith('<%= escape_javascript(render(:partial => "contact", :locals => { :contact => @contact })) %>');
$('#contact_<%=@contact.id%>').effect("highlight", {}, 3000);